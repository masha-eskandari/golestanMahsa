{
  "name": "golestan",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass":"npx sass --watch ./src/scss/main.scss:./src/css/main.css" ,
    "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "sass": "^1.89.2",
    "vite": "^7.0.4"
  }
}
